# Core dependencies
pandas>=2.0.0
numpy>=1.24.0

# OpenAI SDK (with async support)
openai>=1.0.0

# Weaviate client
weaviate-client>=3.24.0

# HTTP clients
aiohttp>=3.9.0
requests>=2.31.0

# Progress bar
tqdm>=4.66.0

# Performance testing
locust>=2.15.0
grequests>=0.7.0  # For gevent-based parallel requests (Locust-compatible)

# API server for parallel search
fastapi>=0.104.0
uvicorn>=0.24.0

# Azure Blob Storage (for backups)
azure-storage-blob>=12.19.0

# Optional: For better performance
uvloop>=0.19.0; sys_platform != 'win32'

